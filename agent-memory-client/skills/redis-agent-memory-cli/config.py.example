# Redis Agent Memory CLI Configuration

# Server Configuration
BASE_URL = "http://localhost:8000"

# Authentication (optional - leave empty for development)
API_KEY = ""

# Request timeout in seconds
TIMEOUT = 30

# Development mode (disables auth)
DISABLE_AUTH = True