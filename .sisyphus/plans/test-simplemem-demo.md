# Plan: Test Demo for SimpleMem Engine in agent-memory-server

## TL;DR
Execute an end-to-end test of the SimpleMem demo flow (extraction, storage, search, and answer generation) using a mocked LLM to ensure deterministic results. Capture outputs, verify memory entries, and confirm the demo script runs without errors.

## Context
- Current integration: SimpleMem engine integrated into agent-memory-server with RedisVL-based long-term memory.
- Objective: Provide a repeatable, low-friction test that demonstrates the demo's capabilities and regression-free behavior.

## Work Objectives
- Core Objective: Validate end-to-end demo path for SimpleMem demo script, including memory write, search, and answer generation steps.
- Deliverables: test report, sample console outputs, and logs; optional disabled-LLM variant for CI.
- Definition of Done: All demo steps complete; memory entries created in RedisVL; semantic/keyword/hybrid searches return results; answer generation returns a structured answer or string; no uncaught exceptions.
- Must Have: deterministic outputs (via mock LLM), reproducible environment, clean teardown.
- Must NOT Have: external API calls to live OpenAI in CI (must be mocked).

## Verification Strategy
- Test Decision: Use a mocked LLM client to return pre-defined responses for embeddings, prompts, and memory extractions.
- QA Scenarios: Happy path (normal dialogues), Edge case (empty dialogues), Performance scenario (bulk dialogue insertion).
- Evidence: store artifacts under .sisyphus/evidence/test-simplemem-demo-<n>.* (logs, stdout captures, JSON results).

## Execution Strategy
### Wave 1: Environment Setup
- Create a clean RedisVL namespace (e.g., namespace="demo-test").
- Ensure Redis server is running (docker-compose or local Redis).
- Configure environment variables for test: set DISABLE_AUTH=true, OPENAI_API_KEY (mocked), MEMORY_SERVER_URL.

### Wave 2: Run Demo with Mocked LLM
- Use a small, deterministic memory scenario:
  - Add several dialogues via SimpleMemEngine
  - Process to persist memories
  - Run semantic/keyword/hybrid search
- Mock LLM calls: Patch LLMClient.chat_completion and LLMClient.extract_json to return fixed JSON structures for answers and extractions.
- Capture console output to .sisyphus/evidence/test-simplemem-demo-w1.txt

### Wave 3: Verify Outputs
- Check that memory entries exist in RedisVL index for the demo namespace.
- Validate search results contain expected fields (lossless_restatement, keywords, timestamp, entities, topic).
- Validate answer generation returns a JSON with expected keys or a concise string per your configuration.
- Validate native long-term memory path (optional) does not break when LLM is mocked.

### Wave 4: Cleanup
- Clear demo namespace data from RedisVL (optional) or drop database for CI cleanliness.
- Remove any temporary files under .sisyphus/evidence.

## Test Data (Sample)
- Dialogues: 4-6 dialogues about a mock meeting or topic.
- Expected memory fields: lossless_restatement, keywords, timestamp, entities, topic.
- Expected search results: non-empty memories.

## Test Harness (Outline)
- A small Python script that:
  - Creates SimpleMemEngine with namespace="demo-test".
  - Adds dialogues and calls process_remaining().
  - Patches LLMClient to a FakeLLM returning deterministic content for memory extraction and answering.
  - Performs semantic/keyword searches and asserts non-empty results.
  - Calls generate_answer and asserts non-empty string/JSON.

## Environment & CI Considerations
- In CI, always mock the LLM to avoid network calls and API keys.
- Use a temporary Redis instance or docker-compose service and reset after tests.
- Ensure test data does not persist between runs unless a dedicated test namespace is used.

## Schedule & Milestones
- Day 1: Implement test harness scaffolding; add mocks.
- Day 2: Run end-to-end demo with mock; verify outputs.
- Day 3: Add a couple of regression scenarios; integrate into CI.

## Acceptance Criteria
- Demo runs without uncaught exceptions.
- Memory entries are created in RedisVL under the test namespace.
- Semantic/Keyword/Hybrid searches return non-empty results.
- Answer generation returns a non-empty result matching expected structure (JSON or text).
- All evidence artifacts are generated and stored under .sisyphus/evidence.

## References
- Demo script: examples/simplemem_demo.py
- Engine code: agent_memory_server/engines/simplemem/
- RedisVL integration: agent_memory_server/memory_vector_db.py

Plan generated by Prometheus planning assistant.
